seed: 42
device: auto

data:
  dataset: mnist
  root: data
  batch_size: 128
  num_workers: 2

model:
  type: cnn
  input_channels: 1
  input_size: 28
  latent_dim: 20

train:
  epochs: 20
  learning_rate: 0.001
  log_interval: 100

output:
  run_dir: experiments/mnist_cnn
  run_name: latent20_e20
